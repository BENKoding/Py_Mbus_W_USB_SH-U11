meta:
  brand: Sensirion
  model: SFC5xxx
  protocol_version: "1.0"
  notes: Exemple de profil — à ajuster selon datasheet.

registers:
  - name: Flow setpoint
    address: 0
    function: 3
    type: f32
    endianness: be
    scale: 1.0
    unit: sccm
    access: RW
    minimum: 0
    maximum: 5000
    critical: true
    description: Consigne de débit.

  - name: Flow measured
    address: 2
    function: 4
    type: f32
    endianness: be
    scale: 1.0
    unit: sccm
    access: RO
    description: Mesure de débit instantanée.

version: "1.0"

